#!/usr/bin/python3

# This program prints all decimal and hexadecimal number from 0 to 96
for num in range(0, 99):
    print(f"{num:d} = {hex(num)}") # The hex function converts and integer to its correspond hex format
